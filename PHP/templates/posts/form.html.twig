{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un nouveau post</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ 'Enregistrer' }}</button>
    {{ form_end(form) }}
{% endblock %}
